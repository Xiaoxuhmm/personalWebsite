import styled from 'styled-components';
import {
  FlexCol
} from '../../components/globals/index';

export const ErrorWrapper = styled(FlexCol)`
`;

export const InfoWrapper = styled(FlexCol)`
  width: 50%;
  max-width: 600px;
  margin: auto;
`
